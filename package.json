{
  "name": "personapp",
  "version": "1.0.0",
  "description": "a nodejs crud app with inbuilt json db",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "author": "Nikhil P",
  "engines": {
    "node": ">=8.1"
  },
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.13.0"
  }
}
